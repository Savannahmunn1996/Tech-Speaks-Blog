<nav>
    <a class="signout" href="/goodbye">Logout</a>
   
</nav>

<button id="create" href="/createPost">Create A Post</button> 

{{#each blogs}}
<h3 class="tit">{{title}}</h3>
<p class="feature">{{post}}</p>

{{/each}}

<h3 class="tit">{{title}}</h3>
<p class="feature">{{post}}</p>

